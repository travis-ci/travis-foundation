guard 'sass', output: 'source', line_numbers: true, debug_info: false do
  watch %r(^.+\.sass$)
end

guard 'slim', input_root: 'pages', output_root: '_source', slim: { pretty: true } do
  watch(%r'^.+\.slim$')
end
